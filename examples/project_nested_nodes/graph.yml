name: nested_nodes_example
default_storage: ${{ secrets.my_primary_storage }}
nodes:
  - import: basis_marketplace.salesforce.extract.extract_contacts
    parameters:
      api_key: ${{ secrets.salesforce_api_key }}
  - subgraph: subgraph1
    inputs:
      my_subgraph_input: extract_contacts
    parameters:
      limit: 200
  - subgraph: subgraph2
  - reference: someother_public_env/output1
