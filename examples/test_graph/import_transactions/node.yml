description: >
  This is a node that pulls and outputs transactions
input_ports:
  - name: input_stream
    type: stream
    schema: myschema.Schema
output_ports:
  - name: raw_transactions
    type: table
parameter_ports:
  - name: myparam
    type: parameter
    parameter_type: text
    default_value: My default text
script:
  script_type: python
  script_definition: raw_transactions.py
