# Pass in the id="sg"
# hook up and return
#  sg:in->pass:in
#  ssg:out->sg:out
# hook up
#  pass:out->ssg:in
#  pass:out->ssg.pass:in
exposes:
  inputs:
    - subgraph_in
  outputs:
    - subgraph_out
nodes:
  - node_file: pass.py
    inputs:
      - subgraph_in -> pass_in
    outputs:
      - pass_out -> subgraph_in
  - node_file: subgraph/graph.yml
