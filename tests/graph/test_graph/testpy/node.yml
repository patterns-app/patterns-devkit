description: >
  This is my node, it does magical things with data
input_ports:
  - name: input_stream
    type: stream
    schema: myschema.Schema
output_ports:
  - name: charges
    type: table
    schema: myschema.OtherSchema
parameter_ports:
  - name: myparam
    type: parameter
    parameter_type: text
    default_value: My default text
script:
  script_type: python
  script_definition: testpy.py
